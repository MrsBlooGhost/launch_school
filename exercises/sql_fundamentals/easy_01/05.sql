-- Exercise 05: WHERE Clause

-- In this exercise, let's practice filtering the data we want to query. Using a WHERE clause, SELECT records for birds under the age of 5.

SELECT * FROM birds
WHERE age < 5;
